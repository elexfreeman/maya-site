
<!-- SUBSYSTEM: docs-commit-rules -->
#  GIT Commit Protocol — Полное руководство**

## 🌌 1. Общий формат

Каждый коммит — это *запись в журнале миссии*:

```
[STARDATE YYYY.MMDD] <CLASSIFICATION>[<SUBSYSTEM>]: Краткое описание

Полное описание
1 Действие и изменения
 - пункт
 - пункт
2 Действие и изменения
 - пункт
 - пункт
 - пункт
```

Пример :
```
[STARDATE 2025.1029] FIRST_INIT[server-controllers,client-controllers,client-router]: Изменения в контроллерах

 - GET_FILE_FOR_CLIENT случайным участникам и клиентский контроллер ответа;
 - отправка через SEND_FILE_FOR_CLIENT
Подробности:
 - server/src/ctrl/file_list_ctrl.rs: добавлена отправка запросов GET_FILE_FOR_CLIENT случайным участникам той же сессии для файлов из list_only_in_db;
 - key содержит адрес запрашивающего клиента.
 - client/src/ctrl/get_file_for_client_ctrl.rs: новый контроллер читает файл из локальной папки сессии и отправляет его на сервер по топику SEND_FILE_FOR_CLIENT с ключом {to_addr, file: FileKey}.
 - client/src/sys_ctrl.rs: добавлена подписка на GET_FILE_FOR_CLIENT и вызов контроллера.
 - client/src/ctrl/mod.rs: экспорт нового контроллера.
 - agent_log.md: добавлена запись о выполненных изменениях.
```
---

## 🪐 2. Классификации миссий (типы коммитов)

Каждый тип коммита отражает цель действия — как раздел в журнале *USS Enterprise*.

| Класс                    | Назначение                                              | Используется когда...                           | Аналог conventional commit |
| ------------------------ | ------------------------------------------------------- | ----------------------------------------------- | -------------------------- |
| **FIRST_INIT**        | Добавление нового функционала, модуля, компонента       | создаёшь что-то новое, что расширяет систему    | `feat`                     |
| **SYSTEM_CALIBRATION**   | Оптимизация, улучшение производительности или точности  | изменяешь алгоритмы, ускоряешь код              | `perf`, `enhancement`      |
| **TEMPORAL_ADJUSTMENT**  | Исправление ошибок, стабилизация системы                | устраняешь баг, ошибку, неправильное поведение  | `fix`                      |
| **CODE_RESTRUCTURING**   | Переписывание внутренней логики без изменения поведения | чистишь архитектуру, делаешь код чище           | `refactor`                 |
| **DATA_LOG_UPDATE**      | Обновление документации, комментариев, README           | добавляешь/исправляешь текст, инструкции, схемы | `docs`                     |
| **CLEANSE_OPERATION**    | Удаление ненужных элементов, мусора, старого кода       | чистишь репозиторий, удаляешь legacy            | `chore`, `cleanup`         |
| **HOLODECK_SIMULATION**  | Добавление или обновление тестов                        | работаешь с тестами, моделями, QA               | `test`                     |
| **TRANSMISSION**         | Деплой, сборка, публикация, передача данных             | отправляешь билд или релиз наружу               | `deploy`, `build`          |
| **MERGE_PROTOCOL**       | Слияние веток или кода из других миссий                 | выполняешь merge, rebase или sync               | `merge`                    |
| **RED_ALERT**            | Срочные исправления критических сбоев                   | чинить нужно *немедленно*                       | `hotfix`                   |
| **TEMPORAL_RESET**       | Откат к предыдущему состоянию                           | отменяешь неудачные изменения                   | `revert`                   |
| **STARFLEET_AUTOMATION** | Автоматические коммиты от CI/CD, скриптов, агентов      | коммиты без участия человека                    | `ci`, `bot`                |

---

## 🧭 3. Подсистемы (SUBSYSTEM)

Используются в квадратных скобках после классификации, чтобы указать область миссии.
Это помогает фильтровать и искать коммиты по модулям.

Примеры:

```
[STARDATE 2025.1029] FIRST_INIT[warp-core]: добавлен алгоритм стабилизации варп-поля
[STARDATE 2025.1029] SYSTEM_CALIBRATION[life-support]: оптимизирована система распределения воздуха
[STARDATE 2025.1029] TEMPORAL_ADJUSTMENT[api-gateway]: исправлена ошибка маршрутизации сигнала
[STARDATE 2025.1029] DATA_LOG_UPDATE[readme]: обновлены инструкции по активации варп-двигателя
```

### Список SUBSYSTEM проекта 

Ниже перечень принятых значений SUBSYSTEM, основанных на структуре и содержимом репозитория. Используйте наиболее точный вариант при коммитах.

- app-pages: страницы и маршрутизация Nuxt (`pages/`).
- app-components: UI‑компоненты и секции (`components/`).
- server-api: серверные эндпоинты и обработчики (`server/api/`).
- domain-products: бизнес‑логика товаров (`system/products.ts`).
- domain-services: бизнес‑логика услуг (`system/services.ts`).
- db-core: подключение к MySQL, пул и утилиты (`system/mysql.ts`, `system/index.ts`).
- config-app: конфигурация приложения (`nuxt.config.ts`).
- runtime-scripts: npm‑скрипты и метаданные пакета (`package.json`).
- deps-lock: lock‑файл зависимостей (`package-lock.json`).
- assets-global: стили и изображения (`style.css`, `img/`, `public/img/`).
- public-static: прочие статические файлы (`public/`).
- sql-data: SQL‑данные/скрипты (`products.sql`).
- docs: документация и правила (`README.md`, `AGENTS.md`, `info/*`).
- license: лицензия проекта (`LICENSE`).
- env: пример конфигурации окружения (`.env.example`).
- legacy-static: корневой статический шаблон/заглушка (`index.html`).
- generated-build: сгенерированные артефакты (не коммитить) (`.nuxt/`).


---

## 🛰️ 4. Рекомендуемая структура репозитория и веток

Чтобы соответствовать духу *Звёздного флота*, можно адаптировать и структуру:

| Ветка       | Назначение             | Атмосферное имя  |
| ----------- | ---------------------- | ---------------- |
| `main`      | стабильная версия      | `enterprise`     |
| `develop`   | основная разработка    | `starbase`       |
| `feature/*` | новые функции          | `mission/*`      |
| `hotfix/*`  | экстренные исправления | `red-alert/*`    |
| `release/*` | подготовка релиза      | `transmission/*` |

---

## 🧩 5. Примеры последовательности коммитов

```
[STARDATE 2025.1018] FIRST_INIT[telemetry]: добавлен модуль анализа сигналов
[STARDATE 2025.1018] SYSTEM_CALIBRATION[warp-core]: повышена эффективность варп-ускорителя на 15%.
[STARDATE 2025.1018] TEMPORAL_ADJUSTMENT[bridge-ui]: исправлено смещение координат на панели управления.
[STARDATE 2025.1018] DATA_LOG_UPDATE[docs]: обновлено описание warp-протокола.
[STARDATE 2025.1018] TRANSMISSION: версия 1.2.0 успешно развернута на станции Deep Space Nine.
```

---

## ⚡ 6. Командный стиль и правила

* Все коммиты — **в настоящем времени** (*«добавлен», «исправлен», «удалён»*).
* Не пиши длинных фраз в начале — максимум 80 символов до конца строки.
* Вторая строка пропускается, полное описание начинается с третьей строки
* Подробно описывай изменения в полном описании
* В описаниях *всегда отражай цель миссии*, не процесс.
* Использовать многострочные коментарии

---

## 🧠 7. Примеры из реальных сценариев

**Разработка:**

```
[STARDATE 2025.1018] FIRST_INIT[auth]: внедрён модуль авторизации
```

**Багфикс:**

```
[STARDATE 2025.1018] TEMPORAL_ADJUSTMENT[replicator]: устранён сбой генерации кофе
```

**Оптимизация:**

```
[STARDATE 2025.1018] SYSTEM_CALIBRATION[sensors]: увеличена дальность сканирования
```

**Удаление мусора:**

```
[STARDATE 2025.1018] CLEANSE_OPERATION[logs]: удалены старые записи
```

**Документация:**

```
[STARDATE 2025.1018] DATA_LOG_UPDATE: обновлены протоколы запуска
```
